
  // if (paragraph.innerText.length === 1) clearDisplay();
    // let afterDel = "";
    // for (let i = 0; i < paragraph.innerText.length-1; i++) {
    //     afterDel += paragraph.innerText[i];
    //     if (i === paragraph.innerText.length-2) {
    //         paragraph.innerText = afterDel;
    //     }
    // }